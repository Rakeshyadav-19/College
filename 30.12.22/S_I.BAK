//WAP to calculate Simple Interest
/*Rakesh Yadav
Roll no. 1029*/

#include<stdio.h>
#include<conio.h>
main()
{
float s,p,r,t;
clrscr();
printf("To calculate Intrest Amount:");
printf("\nEnter Money Invested or Borrowed:");
scanf("%f",&p);

if (p>0)
{
printf("Enter Annnual rate of interest:");
scanf("%f",&r);

if (r>0)
{
printf("Enter the length of time(IN YEARS) you Invest or Borrowed:");
scanf("%f",&t);

if (t>0)
{
printf("The interest amount is=%f",(p*r*t)/100);
printf("\nThe total Amount is=%f",p+((p*r*t)/100));
}
else printf("Time cannot be zero");
}
else printf("Rate of interest cannot be nrgative or zero");
}
 else printf("Money cannot be Zero or Negative");
getch();
}

